import { Meteor } from 'meteor/meteor';
import '../../api/events.js';
import '../../api/usersevents.js';
import '../../api/comments.js';
import '../../api/users.js';

Meteor.startup(() => {
  // code to run on server at startup
});
